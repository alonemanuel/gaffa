{
  "name": "gaffa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5180 --strictPort --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5180"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
